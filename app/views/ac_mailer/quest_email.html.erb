<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>

  <h3>Founders Hookup Accelerator Application</h3>
  <% if !@acnames.nil? %>
    <p>The application below was sent via Founders Hookup to Accelerators:
    </p>
    <ul>
      <% @acnames.each do |acname| %>
        <li> <%= acname %> </li>
      <% end %>
    </ul>
  <% else %>
    <p>The application below was sent via Founders Hookup.
    </p>
  <% end %>
  <br/>

  <%= render :partial => "qinfo" %>

  </body>
</html>
